<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/global.css">
    <link rel="stylesheet" href="/styles/room.css">
    <link rel="stylesheet" href="/styles/modal.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans&family=Poppins:wght@400;500;700&display=swap"
        rel="stylesheet">
    <!-- Informar para a tag script que estamos usando módulos  -->
    <script src="/scripts/main.js" defer type='module'></script>
    <title>Rocket.Q - Perguntas e Respostas</title>
</head>

<body>
    <div id='room' class="content">
        <header>
            <a href="/home">
                <img src="/./images/logo.svg" alt="Rocket.Q Logo">
            </a>
            <div class="buttons">
                <div class="button outlined" id="room-id" data-id="<%= roomId %>">
                    #<%= roomId %>
                        <img src="/./images/copy.svg" alt="Copiar número da sala">
                </div>
                <a href="/create-pass" class="button">
                    <img src="/./images/wusers.svg" alt="Criar Sala">
                    Criar Sala
                </a>
            </div>
        </header>

        <main>
            <section id='question-form'>
                <h2>Faça sua pergunta</h2>
                <form action="/question/create/<%= roomId %>" method="POST" id="category">
                    <div id="qtype">
                        <p id="select">Escolha a categoria:</p>
                        <select name="category" id="category">
                            <option name='category' value="html">HTML</option>
                            <option name='category' value="css">CSS</option>
                            <option name='category' value="js">JS</option>
                        </select>
                    </div>

                    <label class="sr-only" for="question">O que você quer perguntar?</label>
                    <textarea name="question" id="question" placeholder="O que você quer perguntar?"></textarea>
                    <footer>
                        <div>
                            <img src="/./images/locker.svg" alt="Cadeado Perguntas">
                            Esta pergunta é anônima
                        </div>
                        <button>Enviar</button>
                    </footer>
                </form>
            </section>

            <section id="filter">
                <form action="/room/filter/<%= roomId %>" method="GET">
                    <input type="text" name="filter" placeholder="Filtrar por categoria">
                    <button type="submit">Filtrar</button>
                </form>
            </section>

            <section id="questions">
                <h2 class="sr-only">Perguntas da comunidade</h2>
                <div class="questions">

                    <% if (isNoQuestions) { %>
                        <%- include('parts/no-question') %>
                            <%}%>

                                <% questions.forEach(question=> { %>
                                    <div class="question-wrapper">
                                        <div class="question-content">
                                            <div class="user">
                                                <img src="/images/avatar.svg" alt="Avatar do Usuário">
                                            </div>
                                            <div class="question">
                                                <p>
                                                    <%= question.title %>
                                                </p>
                                            </div>
                                        </div>
                                        <div class="actions">
                                            <a href="#" class='check' data-id="<%= question.id %>">
                                                <img src="/images/check.svg" alt="Marcar como lida">
                                                Marcar como lida
                                            </a>
                                            <a href="#" class='delete' data-id="<%= question.id %>">
                                                <img src="/images/trash.svg" alt="Excluir">
                                                Excluir
                                            </a>
                                        </div>
                                    </div>
                                    <% }); %>

                                        <% questionsRead.forEach(questionRead=> { %>
                                            <div class="question-wrapper read">
                                                <div class="question-content">
                                                    <div class="user">
                                                        <img src="/images/avatar.svg" alt="Avatar do Usuário">
                                                    </div>
                                                    <div class="question">
                                                        <p>
                                                            <%= questionRead.title %>
                                                        </p>
                                                    </div>
                                                </div>
                                                <div class="actions">
                                                    <a href="#" class='delete' data-id="<%= questionRead.id %>">
                                                        <img src="/images/trash.svg" alt="Excluir">
                                                        Excluir
                                                    </a>
                                                </div>
                                            </div>

                                            <% }); %>


                </div>
            </section>
        </main>
    </div>

    <div class="modal-wrapper">
        <div class="modal">
            <h2>Excluir Pergunta</h2>
            <p>Tem certeza que você deseja excluir essa pergunta?</p>
            <form action="#" method="POST">
                <label for-id="password" class="sr-only">Digite sua senha admin</label>
                <input id="password" type="password" name="password" placeholder="Digite sua senha">
                <div class="buttons">
                    <div class="button grey cancel">Cancelar</div>
                    <button class="red">Sim, excluir</button>
                </div>
            </form>
        </div>
    </div>

</body>

</html>